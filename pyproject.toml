[project]
name = "cg-bd-sim"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "numpy",
  "scipy",
  "numba",
  "matplotlib",
  "pyyaml",
  "h5py",
]

[project.optional-dependencies]
dev = [
  "pytest",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src_cg_bd_sim*"]